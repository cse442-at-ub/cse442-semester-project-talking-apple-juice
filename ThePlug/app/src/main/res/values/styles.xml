<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">  <!-- removes the toolbar at the top of the screen-->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor" >@color/colorAccent</item>
    </style>

    <style name="lightTheme" parent="AppTheme">
        <item name="bgColor">#ffffff</item>
        <item name="textColor">#000000</item>
        <item name="metaTextColor">#606060</item>
    </style>

    <style name="darkTheme" parent="AppTheme">
        <item name="bgColor">#535353</item>
        <item name="textColor">#ffffff</item>
        <item name="metaTextColor">#bababa</item>
    </style>

</resources>
